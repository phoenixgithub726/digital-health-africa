export const getSelectedNav = (store) => {
    return store.ConsultantDashboardReducer.consultantSelectedNav
}

export const getConsultantData = (store) => {
    return store.ConsultantDashboardReducer.consultantData
}